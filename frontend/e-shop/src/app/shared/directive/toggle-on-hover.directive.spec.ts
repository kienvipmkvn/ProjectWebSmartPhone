import { ToggleOnHoverDirective } from './toggle-on-hover.directive';

describe('ToggleOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
